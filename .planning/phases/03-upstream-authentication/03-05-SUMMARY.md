# Phase 03 Plan 05: Strategy Factory and Integration Summary

**Phase:** 03-upstream-authentication
**Plan:** 05
**Completed:** 2026-02-03
**Duration:** ~5 minutes

## One-liner

Wired auth strategies into composition engine with fail-fast config compilation and per-upstream RoundTripper injection.

## What Was Built

### Auth Strategy Building at Config Compile Time
- Added `Build(ctx context.Context) (Strategy, error)` method to `auth.Config`
- Added `CompiledUpstream` struct containing `Upstream` + `auth.Strategy`
- Updated `CompileConfig` to accept context and build auth strategies for each upstream
- Strategies built at startup for fail-fast validation (missing env vars, invalid OAuth2 credentials)

### Per-Upstream Auth RoundTripper
- Updated `ExecuteStep` signature to accept `*CompiledUpstream` instead of `*Upstream`
- Wraps base transport with auth RoundTripper when strategy is configured
- Creates new `http.Client` per request while reusing underlying connection pool
- Auth strategies inject credentials transparently (header, basic, bearer, or passthrough)

### Passthrough Auth Error Handling
- Added `auth.IsMissingAuthHeaderError()` check in handler's `ServeHTTP`
- Returns 401 Unauthorized with `WWW-Authenticate: Bearer` for missing client auth
- Other auth errors (OAuth2 failure, network) return 502 Bad Gateway
- Added Authorization header to propagated headers for passthrough support

## Key Files Changed

| File | Change |
|------|--------|
| `internal/auth/auth.go` | Added `Build()` factory method |
| `internal/composition/parser.go` | Added `CompiledUpstream`, updated `CompileConfig` signature |
| `internal/composition/step.go` | Updated `ExecuteStep` for auth-aware client, Authorization propagation |
| `internal/composition/executor.go` | Pass `CompiledUpstream` to `ExecuteStep` |
| `internal/composition/handler.go` | Passthrough auth error handling (401) |
| `internal/composition/parser_test.go` | Auth compilation tests |
| `internal/composition/handler_test.go` | Handler auth tests |

## Commits

| Hash | Message |
|------|---------|
| 4ea29d4 | feat(03-05): build auth strategies during config compilation |
| 8dc402f | feat(03-05): apply auth RoundTripper per upstream in step execution |
| fd30b16 | feat(03-05): handle passthrough auth errors in handler |

## Decisions Made

1. **Context parameter for CompileConfig**: OAuth2 strategy needs context for initial token fetch during fail-fast validation at startup
2. **Authorization header propagation**: Added to propagated headers list - other auth strategies override via RoundTripper anyway
3. **Per-request http.Client**: Creates new Client with auth transport per request, but reuses underlying connection pool (transport is stateless)

## Deviations from Plan

None - plan executed as written.

## Test Coverage

- `TestCompileConfig_AuthHeaderStrategy` - Header auth compiled at startup
- `TestCompileConfig_AuthMissingEnvVar` - Missing env var fails at compile time
- `TestCompileConfig_AuthMultipleStrategies` - Multiple strategies rejected
- `TestCompileConfig_NoAuth` - Upstreams without auth work normally
- `TestHandler_PassthroughAuthMissing` - Returns 401 with WWW-Authenticate
- `TestHandler_PassthroughAuthPresent` - Forwards auth to upstream
- `TestHandler_HeaderAuth` - Injects configured header

## Phase 3 Completion Status

With Plan 05 complete, Phase 3 (Upstream Authentication) is now COMPLETE.

Requirements delivered:
- AUTH-01: Header strategy (static API key injection)
- AUTH-02: Basic auth strategy
- AUTH-03: Passthrough strategy (forward client auth)
- AUTH-04: OAuth2 token caching with early refresh
- AUTH-05: OAuth2 singleflight protection
- AUTH-06: Per-upstream auth configuration (this plan)

All authentication strategies are now integrated into the composition engine.

## Next Phase Readiness

Phase 4 (Resilience) prerequisites met:
- HTTP client with connection pooling (Phase 1)
- Composition execution with parallel steps (Phase 2)
- Authentication wired per-upstream (Phase 3)

Ready to proceed with Phase 4: timeout, retry, and circuit breaker patterns.

---

*Generated by Claude Code execution*
