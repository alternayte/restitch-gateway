# Compiled binaries
/restitch
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Bundled frontend assets (finding M36): the build produces the whole dist/
# directory; nothing under it is committed, and the Makefile builds the
# frontend before Go steps that embed it.
cmd/restitch-studio/dist/

# Test binary
*.test

# Output of go coverage tool
*.out

# Dependency directories
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# TLS certificates (test)
*.pem

# Studio runtime database (created by `restitch-studio` when run from the repo root)
studio.db
studio.db-shm
studio.db-wal
